# گزارش مهندسی — میلانو تریدینگ اینتلیجنس (نسخهٔ ۵)

**نویسنده/طراح:** Milad Yaghoobi · Meelano Studio Design
**تاریخ:** ۲۰۲۶-۰۹-۱۶ · **نسخه:** 5.1.0 (APEX TRADER+) · **ساختار دیتابیس:** 4.0.0-crypto

این سند، آنالیز فنی کامل بات هوشمند ترید کریپتو را مستند می‌کند: پاک‌سازی نسخهٔ
قبلی، اصلاح باگ‌های بحرانی، معماری موتور نسل پنجم و ارتقای دقت نسل ۵٫۱.

---

## ۰) خلاصهٔ مداخلات نسخهٔ ۵

### ۰٫۱) پاک‌سازی آلودگی از پروژهٔ دیگر

نسخهٔ قبلی بستهٔ ZIP حاوی بقایای یک پروژهٔ دیگر (پنل مدیریت انبار و قیمت‌گذاری
کالای فیزیکی با ترب/دیجی‌کالا) بود که به‌صورت جزئی به کریپتو تغییر کاربری داده
شد اما ردپاهای فراوانی باقی مانده بود. موارد پاک‌سازی‌شده:

| # | آلودگی | اقدام |
|---|---|---|
| 1 | ۱۱ وظیفهٔ AI نامرتبط (`product.normalize`, `seo.*`, `translate.i18n`, `image.product`, `video.product`, `embed.similarity`, …) | حذف؛ فقط ۴ وظیفهٔ کریپتو باقی ماند (`crypto.signal/review/regime/report`) |
| 2 | ۶ ارائه‌دهندهٔ تصویر/ویدیو (Stability، DeepAI، fal، Kling، PiAPI، BytePlus) | حذف کامل + حذف ~۵۰۰ خط پروتکل تصویر/ویدیو/امبدینگ از `Ai\Client` |
| 3 | `market.sources = [torob, digikala]` در تنظیمات و API ذخیره | حذف؛ لایهٔ بازار اکنون Binance + CoinGecko با کش |
| 4 | پیام سیستمی AI: «دستیار پنل مدیریت انبار و قیمت‌گذاری بازار ایران» | بازنویسی به پرسونای «معامله‌گر نهادی کریپتو» |
| 5 | فیلدهای `image_model`/`video_model` در UI تنظیمات و API | حذف |
| 6 | مستندات README/ANALYSIS توصیف‌کنندهٔ پنل انبار | بازنویسی کامل |
| 7 | عنوان فوتر: «سامانه‌های هوشمند بازار و انبار» | اصلاح |

### ۰٫۲) باگ‌های بحرانی اصلاح‌شده (یافت‌شده از لاگ واقعی سرور)

| # | باگ | شاهد در لاگ | اصلاح |
|---|---|---|---|
| 1 | `Security::audit()` ستون `actor` را insert می‌کرد ولی ستون جدول `user` است → **همهٔ رویدادهای ممیزی شکست می‌خوردند** | `SQLSTATE[42S22]: Unknown column 'actor'` (ده‌ها بار) | ستون به `user` اصلاح شد + تست |
| 2 | کارنامهٔ AI در جدول `ai_tasks` (جدول *تعریف* وظایف با ستون‌های نامربوط) نوشته می‌شد → **هیچ لاگ اجرا ثبت نمی‌شد** | `Unknown column 'task' in 'INSERT INTO'` | جدول جدید `ai_runs` + اصلاح نگاشت |
| 3 | `Installer.php` از `$def['title']` استفاده می‌کرد ولی کلید Schema نامش `label` است | PHP Warning × خطوط ۷۲/۷۴/۱۰۹/۱۱۴ | اصلاح ۴ نقطه |
| 4 | مدل پیش‌فرض Groq (`llama-3.3-70b-versatile`) منسوخ شده و پاسخ «وجود ندارد» می‌داد | `The model … does not exist` | پیش‌فرض به `llama-3.1-8b-instant` + قابل تغییر از UI |
| 5 | **رمز دیتابیس واقعی به‌صورت هاردکد در `Config::defaults()`** (`Milad@1369`) | — | حذف کامل؛ پیش‌فرض خالی + تست |
| 6 | ZIP توزیع شامل `config/settings.php` (کلیدها رمزشده) **و `config/.app_key` (خود کلید AES!)** و لاگ‌های اجرایی | — | از مخزن حذف شدند؛ چون در تاریخ گیت باقی‌اند، همهٔ کلیدها باید rotate شوند |

---

## ۱) معماری موتور نسل ۵ — «قیف تصمیم نهادی»

```
                     ┌────────────────────────────────────────────┐
                     │  رصد کل بازار (Binance 24hr → پرحجم‌ترین‌ها) │
                     └──────────────────┬─────────────────────────┘
                                        ▼
   ① پیش‌فیلتر نقدینگی (حجم ۲۴س ≥ حد)
                                        ▼
   ② بافت تایم‌فریم اصلی (Context: ۲۵+ اندیکاتور یک‌بار محاسبه)
                                        ▼
   ③ رژیم بازار (Regime: trend_up / trend_down / range / volatile)
                                        ▼
   ④ ۱۵ فیلتر هم‌گرایی وزن‌دار — وزن‌ها تابع رژیم
                                        ▼
   ⑤ آستانه‌های سخت: ≥۱۱ فیلتر + امتیاز تکنیکال ≥۶۲
                                        ▼
   ⑥ تأیید چند تایم‌فریمی (1h/4h/1d وزنی) — دروازهٔ جهت
                                        ▼
   ⑦ اجماع ۳مدلی AI (پرامپت نهادی: رژیم+MTF+ریسک+ابطال)
                                        ▼
   ⑧ دروازهٔ بیت‌کوین (BTC 4h نزولی قوی ⇒ لانگ فقط A+)
                                        ▼
   ⑨ پلن ریسک ساختاری + حداقل R:R ≥ ۲
                                        ▼
      درجه‌بندی A+/A/B/C → خنک‌کردن ۱۲س → ذخیره و اعلام
```

### ۱٫۱) لایهٔ ریاضی (`Indicators` + `Context`)

همهٔ اندیکاتورها **خالص و بدون وابستگی** روی آرایه‌های عددی پیاده شده‌اند تا
قابل تست و بازتولید باشند. `Context::series()` هر سری را یک‌بار محاسبه و
`Context::snapshot($i)` مقدار «همان لحظه» را بدون دیدن آینده برمی‌گرداند —
این یعنی موتور زنده و بک‌تست از یک ریاضی واحد تغذیه می‌شوند (قابلیت حسابرسی).

مجموعهٔ اندیکاتورها: SMA، EMA(9/21/50/200)، RSI (وایلدر)، MACD، بولینگر (+عرض
باند)، استوکستیک، ATR (وایلدر)، **ADX/DI (وایلدر)**، **OBV**، **VWAP غلتان**،
**Supertrend**، **شیب رگرسیون نرمال‌شده**، **رتبهٔ صدکی**، ساختار سقف/کف،
سوئینگ‌های ۲۰ کندلی و نسبت سایهٔ منیپولیشن.

### ۱٫۲) رژیم بازار (`Regime`)

| رژیم | شرط تشخیص | اثر |
|---|---|---|
| `trend_up/down` | ADX ≥ ۲۰ و \|DI+−DI−\| ≥ ۶ (یا ADX ≥ ۲۵ + هم‌جهتی EMA/ساختار) | وزن فیلترهای روندی ×۱٫۳، سایز کامل |
| `range` | عرض باند صدک ≤ ۳۰ یا ADX < ۱۸ | وزن فیلترهای بازگشت به میانگین ×۱٫۳، سایز ×۰٫۷ |
| `volatile` | ATR٪ ≥ ۴ + صدک عرض باند ≥ ۸۵ (یا ATR٪ ≥ ۶) | سایز ×۰٫۵۵، حالت دفاعی |

### ۱٫۳) امتیاز تکنیکال

```
tech_score = ۵۵ × (سهم رأی وزنی جهت غالب) + ۳۰ × (نسبت فیلترهای عبوری) + ۱۵ × (نسبت هم‌گرایی جهت‌دار)
```

### ۱٫۴) پلن ریسک (`RiskManager`)

- **استاپ ساختاری:** پشت کف/سقف سوئینگ با بافر ۰٫۲۵×ATR؛ اگر دورتر از سقف
  مجاز (۳×ATR) بود → استاپ ATR (۲×ATR).
- **نردبان سود:** 1.5R / 2.5R / 4R + انتقال استاپ به سربه‌سر پس از TP1.
- **سایز:** `position٪ = (ریسک٪ / فاصلهٔ استاپ٪) × ۱۰۰ × ضریب رژیم × ضریب اعتماد` با سقف.
- فراداده: اهرم پیشنهادی (۱..۳×)، مدت نگهداری تخمینی (کندل)، شرط ابطال.

### ۱٫۵) بک‌تست (`Backtest`) — صداقت روش‌شناختی

- اندیکاتورها یک‌بار محاسبه می‌شوند؛ چون هر مقدار در i فقط از داده‌های ≤ i
  ساخته می‌شود، هیچ نشتی از آینده وجود ندارد.
- ورود در **OPEN کندل بعدی**؛ استاپ **اولویت** بر تارگت دارد (محافظه‌کارانه)؛
  کارمزد رفت‌وبرگشت (پیش‌فرض ۸ بی‌پی‌اس) در واحد R کسر می‌شود.
- خروجی: تعداد معامله، وین‌ریت، امید ریاضی (R)، پروفایت فاکتور، حداکثر افت،
  منحنی سرمایه و ۴۰ معاملهٔ آخر.
- **محدودیت اعلام‌شده:** فقط لایهٔ تکنیکال؛ اجماع AI در گذشته قابل بازتولید نیست.

### ۱٫۶) لایهٔ AI

- ۷ ارائه‌دهندهٔ متنی با ۳ پروتکل (OpenAI / Gemini / Cloudflare) + مسیریاب
  امتیازده (تخصص + سلامت + سرعت + هزینه + دستی).
- `AiValidator` پنل ۳ مدل برتر را با پرامپت نهادی (شامل رژیم، MTF، پلن ریسک)
  فراخوانی می‌کند؛ رأی‌گیری وزنی با اعتماد مدل‌ها + شرط ابطال.
- کارنامهٔ کامل هر فراخوانی در `ai_runs` (تأخیر، توکن، وضعیت HTTP، هش ورودی).

### ۱٫۷) دادهٔ بازار (`MarketData`)

Binance (primary) + CoinGecko (fallback). کش دو لایه: حافظهٔ درون‌درخواست
(برای MTF که همان کندل را در ۳ تایم‌فریم می‌خواهد) + کش فایلی TTL دار در
`storage/cache` برای احترام به rate-limit صرافی. سقوط کنترل‌شده در خطا؛ هرگز
دادهٔ جعلی جا زده نمی‌شود.

### ۱٫۸) ارتقای دقت نسخهٔ ۵٫۱ — «پنج لایه، چشم به گذشته، ریسک پرتفوی»

نسخهٔ ۵٫۱ سه ضعف بنیادی نسل ۵ را ترمیم و پنج لایهٔ اطلاعاتی جدید افزود:

| # | اصلاح/افزوده | شرح فنی |
|---|---|---|
| ۱ | **فقط کندل بسته** | `MarketData::candles()` کندلِ در حال شکل‌گیری را حذف می‌کند (`time + intervalSec > now`). محاسبهٔ اندیکاتور روی کندل باز، منبع ۲۰–۳۰٪ سیگنال‌های کاذب است چون مقادیر تا بسته‌شدن مدام تغییر می‌کنند |
| ۲ | **SignalTracker (چشم به گذشته)** | هر سیگنال ذخیره‌شده با کندل‌های واقعیِ بعد از انتشار داوری می‌شود: TP1/TP2/TP3/استاپ/تایم‌اوت، R خالص (کارمزد+اسلیپیج کسرشده) با مدل خروج ۵۰/۲۵/۲۵ و انتقال استاپ به سربه‌سر بعد از TP1. خروجی: وین‌ریت واقعی هر (درجه × رژیم) → «اعتماد کالیبره» روی کارت سیگنال + تزریق خودکار در پرامپت AI («از N سیگنال مشابه، X٪ به TP1 رسیدند») |
| ۳ | **دروازهٔ همبستگی پرتفوی** | آلت‌کوین‌ها همبسته‌اند؛ اسکن سقف نماد هم‌جهت (`max_same_side`) و سایز تجمعی (`max_portfolio_position_pct`) را اعمال می‌کند و از میان کاندیدها برترین‌ها را نگه می‌دارد |
| ۴ | **۱۰ فیلتر جدید (مجموع ۲۵)** | RS/BTC (قدرت نسبی)، واگرایی RSI+OBV، شکار نقدینگی (سویپ استاپ)، نسبت کارایی کافمن، FVG (گپ ارزش منصفانه)، سشن معاملاتی، فاندینگ، اوپن اینترست (بارگذاری تنبل — فقط برای کاندیدهای عبورکرده)، ترس و طمع، POC (گره حجم). فیلترهای دادهٔ خارجی در نبود داده «عبور خنثی» می‌شوند (هرگز مسدود نمی‌کنند) |
| ۵ | **وکیل مدافع AI (Red-Team)** | پس از اجماع، یک مدل با پرامپت بدبینانه حفره‌های ستاپ را می‌جوید (`crypto.review`); رأی INVALID با اعتماد ≥۷۰ = وتوی کامل. + **خودسازگاری**: مدل برتر پرسش را دوباره می‌شنود؛ ناسازگاری دو پاسخ = حذف رأی مدل |
| ۶ | **اسلیپیج + تفکیک رژیمی** | بک‌تست با اسلیپیج هر سمت (پیش‌فرض ۳ بی‌پی‌اس) روی ورود و خروج‌ها + گزارش عملکرد به تفکیک رژیم |
| ۷ | **Robustness نهادی** | Walk-Forward (پنجره‌های زمانی، حکم robust/mixed/fragile) + مونت‌کارلو (۵۰۰ بازچینی ترتیب معاملات، صدک‌های R نهایی و حداکثر افت و احتمال ضرر؛ بذر ثابت = قابل بازتولید) |
| ۸ | **اصلاح باگ پرامپت AI** | مقادیر اندیکاتور (RSI/ADX/EMA/…) قبلاً به‌صورت تودرتو ارسال می‌شدند و مدل آن‌ها را نمی‌دید؛ اکنون سطح اول خلاصه + داده‌های ۵ لایه (فاندینگ/OI/سنتیمنت/ساختار) + سابقهٔ ردیاب در پرامپت حضور دارند |

**قیف نسخهٔ ۵٫۴ (۱۱ مرحله، ۳۱ فیلتر):** پیش‌فیلتر نقدینگی → رژیم → ۲۵ فیلتر (۵ لایه) → آستانه‌های سخت →
دادهٔ OI + ارزیابی مجدد → MTF → اجماع AI + خودسازگاری → وکیل مدافع → دروازهٔ BTC →
R:R ≥ حد → درجه‌بندی + کالیبراسیون تاریخی → دروازهٔ همبستگی پرتفوی + خنک‌کردن.

### ۱٫۹) نسخهٔ ۵٫۲ — «معامله‌گر خودکار: کیف کاغذی، اجرای سیگنال، پایش خروج، اتصال صرافی»

از سیگنال تا معامله: نسخهٔ ۵٫۲ حلقهٔ بازخورد را می‌بندد — سیگنال‌های قیف ۵٫۱ اکنون می‌توانند
به‌صورت خودکار «اجرا» شوند، در کیف کاغذی (یا صرافی واقعی) دنبال شوند و کارنامهٔ واقعی بسازند.

| # | مؤلفه | شرح فنی |
|---|---|---|
| ۱ | **AutoTrader** (`includes/Crypto/AutoTrader.php`) | هستهٔ اجرا: `openPosition()` (دستی/خودکار)، `afterScan()` (قلاب پس از هر اسکن)، `updatePrices()` (پایش خروج)، `state()` (نمای کامل). معاملهٔ خودکار فقط سیگنال‌های با درجه ≥ `auto_min_tier` (C=۱ … A+=۴)، اعتماد ترکیبی ≥ `auto_min_combined`، بدون تکرار نماد باز و زیر سقف `auto_max_open_positions` را می‌بازد |
| ۲ | **کیف کاغذی** | جدول `trade_accounts`: موجودی اولیهٔ دلخواه USDT، `usdt_free`/`usdt_locked`؛ هر بازکردن مبلغ (درصدی از موجودی یا ثابت) را قفل می‌کند و در بستن، عایدی (با کارمزد ۱۱ بی‌پی‌اس هر سمت = ۸ کارمزد + ۳ اسلیپیج) آزاد می‌شود |
| ۳ | **پایش خروج نردبانی** | روی کندلِ بسته: اولویت استاپ → TP1 (۵۰٪ حجم + انتقال استاپ به سربه‌سر — فقط در حالت `ladder`) → TP2 (۲۵٪) → TP3 (باقی) + بستن خودکار با سیگنال مخالف (`auto_close_on_opposite`) |
| ۴ | **سه حالت اجرا** | `buy_sell` (کامل)، `buy_only`، `sell_only` — سیگنال‌های ورود/خروج مطابق حالت فیلتر می‌شوند؛ Dry-Run پیش‌فرض است (فردار، بدون اثر) |
| ۵ | **کارنامه و منحنی** | جدول `trade_trades` با PnL دقیق (مبلغ + درصد + R واقعی)، مدت نگهداری، دلیل خروج؛ `state()` منحنی سرمایه، آمار کل (وین‌ریت، سود ناخالص/ناخالص، بهترین/بدترین، میانگین R) و تفکیک به‌ازای نماد برمی‌گرداند |
| ۶ | **Connector/BinanceSpot** | اینترفیس `Connector` + پیاده‌سازی Binance Spot: امضای HMAC-SHA256، نرمال‌سازی `lotStep`/`roundToStep`، `marketOrder` با `quoteOrderQty`، سوییچ تست‌نت/اصلی (`testnet.binance.vision`). کلیدها با AES-GCM (کلید `.app_key`) ذخیره و در پاسخ‌ها ماسک می‌شوند |
| ۷ | **معاملهٔ واقعی، دومرحله‌ای** | `exchange.live_enabled` پیش‌فرض قفل است؛ فعال‌سازی نیازمند ذخیرهٔ کلید + تایپ دقیق `ENABLE-LIVE` است؛ تغییر کلید = قفل مجدد. `auto_dry_run` جدا از آن، لایهٔ دوم محافظ است |
| ۸ | **پنل `trade.php`** | ۵ کارت کیف، کارت‌های حالت اجرا، سوییچ‌های TP/استاپ، نمودار منحنی سرمایه/دونات/میلهٔ نماد (SVG، هم‌آهنگ تم)، جدول پوزیشن/کارنامهٔ زنده، بخش اتصال صرافی — همه با رفرش خودکار ۶۰ ثانیه |

### ۱٫۱۱) نسخهٔ ۵٫۴ — «Precision Edge: حسابرسی قیف و لایهٔ ششم تأیید اجرا»

بازبینی صفر تا صد موتور با نگاه میز معاملاتی حرفه‌ای؛ خروجی: ۳ باگ واقعی رفع و ۶ شاهد جدید.

| # | یافته/اصلاح | شرح فنی |
|---|---|---|
| ۱ | **باگ: RSI نابینا به رژیم** | RSI≤۳۰ در همهٔ رژیم‌ها رأی «فروش» می‌داد؛ در بازار رِنج این تضاد مستقیم با منطق بازگشت به میانگین (بولینگر/استوکستیک) است و آرای هر دو جهت را می‌شکست. اکنون: روند = مومنتوم ادامه‌دار، رِنج = اشباع فروش یعنی خرید میانگین‌گرا، پرنوسان = محافظه‌کار |
| ۲ | **باگ: برچسب استاپ ساختاری** | وقتی استاپ ATR تنگ‌تر بود، نوع «structure» ثبت می‌شد ولی عددِ ATR استفاده می‌شد (سوگیری آماری کارنامه). انتخاب اکنون صادقانه است + کف حداقلی فاصلهٔ استاپ `max(0.5٪ قیمت، 0.9×ATR)` — استاپ تنگ‌تر از نویز بازار، معاملهٔ باختِ قطعی است |
| ۳ | **باگ: آستانهٔ مطلق فیلترها** | `min_filters_passed=17` با رشد تعداد شاهدها نسبتاً سست می‌شد. دروازهٔ تطبیقی: `max(تنظیم، ⌈۶۲٪ کل⌉)` در موتور زنده و بک‌تست + رفع کهنگی `tech_side/confluence` پس از ارزیابی مجدد OI |
| ۴ | **فیلتر ۲۶: بریدگی (CHOP-14)** | `100×log₁₀(ΣTR/دامنه)/log₁₀(14)` — انرژی جهت‌دار واقعی؛ در روند CHOP≤55 لازم، در رِنج CHOP≥35. مکمل ADX با حساسیت بالاتر به نویز |
| ۵ | **فیلتر ۲۷: ابر ایچیموکو** | قیمت نسبت به ابر (با شیفت استاندارد ۲۶ کندله، بدون نشت آینده) + تنکان/کیجون — جهت‌نمای ساختاری مورد استفادهٔ میزهای نهادی |
| ۶ | **فیلتر ۲۸: ضدتعقیب (Anti-Chase)** | فاصلهٔ قیمت از EMA21 بر حسب ATR: سالم (−2.5..+3)، کشیده (تا ±5) با امتیاز کم، پارابولیک در رِنج = رد. بزرگ‌ترین منبع ضرر خرد = خرید در انتهای موج هیجانی |
| ۷ | **فیلتر ۲۹: الگوی کندل** | انگالفینگ/چکش/ستارهٔ ثاقب روی کندل بسته — تأیید زمان‌بندی ورود |
| ۸ | **فیلتر ۳۰: قدرت پایانه (CLV)** | میانگین ۳ کندلهٔ محل بسته‌شدن در دامنه — هیئت‌رسمی خرید/فروش |
| ۹ | **فیلتر ۳۱: انسداد حجم** | حجم ≥۴×: با سایهٔ بلند بالا = اوج دمیده‌شده (رأی فروش)، با بسته‌شدن در کف = فلش تسلیم (رأی خرید)، مبهم = عدم عبور تا تثبیت |

### ۱٫۱۰) نسخهٔ ۵٫۳ — «اطلاع‌رسانی چندکاناله: از رویداد تا پیام»

حلقهٔ دوم بازخورد بسته می‌شود: هر رویداد سامانه (سیگنال، باز/بستن پوزیشن، گزارش کیف)
اکنون به‌صورت خودکار به پیام‌رسان‌ها می‌رسد.

| # | مؤلفه | شرح فنی |
|---|---|---|
| ۱ | **Notifier** (`includes/Crypto/Notifier.php`) | ارکستراتور: ۴ رویداد (`signal`, `trade_opened`, `trade_closed`, `wallet_report`)؛ قالب‌های فارسی کامل/فشرده (پیامک)؛ dispatch به همهٔ کانال‌های فعالِ مشترک با try/catch جدا برای هر کانال (خطای یک کانال بقیه را نمی‌شکند) |
| ۲ | **درایورها** (`includes/Crypto/Notify/`) | قرارداد `Channel` (send/check/configured): **تلگرام** (Bot API رسمی)، **بله** (سازگار با Bot API تلگرام)، **روبیکا** (Bot API با نشانی قابل‌تنظیم)، **واتساپ** (Cloud API متا با Bearer + messaging_product)، **پیامک** (کاوه‌نگار REST). نشانی API همه قابل‌تنظیم برای سازگاری با نسخه‌های آینده |
| ۳ | **اشتراک رویداد** | به‌ازای هر کانال: ۴ سوییچ مستقل (سیگنال/بازشدن/بستن/گزارش کیف) + کلید اصلی + حداقل درجهٔ سیگنال (`min_tier`)؛ سیگنال‌های یک اسکن به‌صورت دسته‌ای در یک پیام می‌روند |
| ۴ | **ضداسپم و لاگ** | throttle به‌ازای (کانال × رویداد) از جدول `notify_log`؛ هر ارسال (موفق/ناموفق) با خطا و پیش‌نمایش ثبت می‌شود؛ `lastByChannel` برای وضعیت UI |
| ۵ | **سلامت و تست** | «تست سلامت» بدون پیام (تلگرام getMe، واتساپ اعتبارسنجی توکن، کاوه‌نگار account/info)؛ «ارسال پیام تست» واقعی؛ پیش‌نمایش پیام با دادهٔ نمونه — همه در پنل |
| ۶ | **امنیت** | توکن‌ها AES-GCM در `config/settings.php`؛ در `status` و `publicView` همیشه ماسک؛ راز خالی در ذخیره = حفظ راز قبلی؛ ممیزی هر تغییر |
| ۷ | **قلاب‌ها** | `api/scan.php` → `notifySignals` پس از اسکن؛ `AutoTrader` (پارامتر اختیاری Notifier) → رویداد باز/بستن از داخل موتور + گزارش کیف اختیاری پس از بستن؛ `api/paper.php` → معاملات دستی هم اطلاع‌رسانی می‌شوند |

---

## ۲) ساختار دیتابیس (Schema 4.0.0-crypto)

| جدول | نقش |
|---|---|
| `scans` | هر دور اسکن + خلاصه (عرض بازار، رژیم BTC) |
| `signals` | سیگنال‌ها با ستون‌های جدید: `tier`, `regime`, `mtf_score`, `mtf_json`, `invalidation` |
| `watchlist` | نمادهای تحت رصد دستی |
| `ai_tasks` | تعریف وظایف AI |
| `ai_runs` ⭐ | کارنامهٔ اجرای موتورهای AI (اصلاح باگ) |
| `ai_health` | تاریخچهٔ تست سلامت ارائه‌دهندگان |
| `settings_audit` | ممیزی تغییرات (اصلاح باگ user/actor) |
| `app_state` | وضعیت اجرایی/مهاجرت |
| `trade_accounts` ⭐ | کیف کاغذی: موجودی اولیه/آزاد/قفل، وضعیت |
| `trade_positions` ⭐ | پوزیشن‌های باز: نماد، ورود، استاپ، نردبان TP، حجم باقیمانده، قیمت نشانگر |
| `trade_trades` ⭐ | کارنامهٔ هر معامله: ورود/خروج، PnL (USDT و ٪)، R، دلیل خروج، منبع (خودکار/دستی) |
| `notify_log` ⭐ | تاریخچهٔ ارسال اطلاع‌رسانی: کانال، رویداد، موفق/خطا، پیش‌نمایش، زمان |

---

## ۳) راستی‌آزمایی

- `php tests/run.php`: **۲۱۵ تست، ۰ خطا** — شامل:
  - اندیکاتورهای جدید (ADX، DI، OBV، VWAP، Supertrend، شیب، سوئینگ)
  - Context (snapshot میانی بدون نشت)
  - رژیم (۴ حالت + ضریب سایز)
  - فیلترها (۱۵ فیلتر، بافت صعودی/ضعیف)
  - ریسک (استاپ ساختاری، نردبان R، تعدیل رژیمی)
  - خط‌لوله کامل end-to-end با MockTransport (اجماع AI، MTF، دروازهٔ BTC، خنک‌کردن)
  - بک‌تست (معامله، وین‌ریت، امید ریاضی مثبت در روند مصنوعی، اسلیپیج، تفکیک رژیمی)
  - اندیکاتورهای ۵٫۱ (ER، واگرایی صعودی/نزولی، FVG، POC، سشن، سویپ، RS)
  - کندل بسته (حذف کندل ناقص) + فاندینگ/OI/ترس‌وطمع از ماک شبکه
  - ردیاب (داوری TP3/استاپ/تایم‌اوت، آمار درجه×رژیم، خط پرامپت، اعتماد کالیبره)
  - دروازهٔ همبستگی پرتفوی (سقف هم‌جهت با ۶ نماد هم‌جهت)
  - وکیل مدافع (وتوی INVALID) + خودسازگاری (حذف رأی ناپایدار)
  - واک‌فوروارد + مونت‌کارلو (ثابتِ مجموع، صدک افت) + ارتقای Installer ستون‌های ردیاب
  - `ai_runs` و ممیزی امنیتی (باگ‌های اصلاح‌شده)
  - رمزنگاری Config + پیش‌فرض بدون رمز دیتابیس
  - AutoTrader (نصب/ریست کیف، بازکردن خودکار با فیلتر درجه، کسر موجودی، ضدتکرار، سقف پوزیشن،
    بستن با سیگنال مخالف، PnL/R/موجودی با کارمزد ۱۱bps، پلهٔ TP1 با سربه‌سر، state/منحنی/آمار،
    Dry-Run، قلاب afterScan)
  - BinanceSpot (roundToStep/qtyToString/signQuery، تست‌نت/اصلی baseUrl، موجودی امضاشده، سفارش بازار quoteOrderQty، عدم افشای راز)
  - تست عملکردی API (هر فراخوانی در پردازش مستقل): احراز 401، state/reset/config، گارد 422،
    status صرافی، قفل دومرحله‌ای ENABLE-LIVE، ماسک کلید — **۱۳/۱۳** ✓
  - Notifier (قالب کامل/فشرده، dispatch پنج‌کاناله با URL/هدر/بدنهٔ درست هر درایور، اشتراک رویداد،
    فیلتر درجه، دسته‌ای، throttle، جداسازی خطا + لاگ، تست سلامت getMe، ذخیره با حفظ راز، ماسک status،
    رویداد باز/بستن از AutoTrader)
  - v5.4 (CHOP روند/زیگزاگ، ایچیموکو با شیفت بدون نشت، انگالفینگ/چکش/ستاره، CLV،
    ضدتعقیب سالم/کشیده/پارابولیک×رژیم، انسداد حجم سه‌حالته، RSI رژیم‌آگاه،
    انتخاب صادقانهٔ استاپ + کف 0.9×ATR، دروازهٔ تطبیقی ۶۲٪)
  - اسموک رندر `trade.php` و `notify.php` با دیتابیس واقعی — **۱۴+۱۴** ✓ · لینت ۶۲ فایل، ۰ خطا
  - تست عملکردی API اطلاع‌رسانی (پردازش مستقل per-call): 401، status/save، حفظ راز، گارد 422،
    check/test/preview، گزارش کیف — **۱۵/۱۵** ✓

## ۴) محدودیت‌های شناخته‌شده (شفافیت)

- فقط Binance/CoinGecko به‌عنوان منبع داده ( abstraction صرافی‌های دیگر در نقشه راه).
- اجرای واقعی فقط Binance Spot (مارکت اردر quoteOrderQty)؛ سایر صرافی‌ها/نوع سفارش در نقشه راه.
- کیف کاغذی کندل‌محور است: اسپرد/لغزش لحظه‌ای با مدل ۱۱ بی‌پی‌اس هر سمت تقریب زده می‌شود.
- اطلاع‌رسانی واتساپ محدودیت پنجرهٔ ۲۴ ساعتهٔ Cloud API متا دارد (گیرنده باید پیام قبلی داده باشد)؛
  نشانی API روبیکا/بله قابل‌تنظیم است تا تغییرات نسخه‌های بعدی بدون تغییر کد جذب شود.
- اجماع AI در بک‌تست شبیه‌سازی نمی‌شود (غیرقابل بازتولید).
- بک‌تست کندل‌محور است؛ اسپرد، لغزش، فاندینگ و partial-fill را مدل نمی‌کند.
- موتور لانگ/شورت متقارن نیست (سمت SELL بیشتر برای خروج/هج است).
- ردیاب فقط لایهٔ قیمت را داوری می‌کند (اسلاید واقعی لحظهٔ سیگنال در دادهٔ کندلی موجود نیست؛ به‌جای آن اسلیپیج پارامتری کسر می‌شود).
- واگرایی «کلاسیک» است؛ واگرایی مخفی (hidden) در نقشه راه.
- خبر/سنتیمنت زنده ندارد (دروازهٔ خبر دستی است).

## ۵) نقشه راه نسخهٔ ۶

1. **اعتبارسنجی:** walk-forward، out-of-sample، مونت‌کارلو روی ترتیب معاملات
2. **داده/اجرا:** abstraction صرافی اجرا (Bybit/OKX)، سفارش لیمیت/OCO، فاندینگ/اوآی/لیکوییدشن
3. **هوش:** طبقه‌بند رژیم آماری (HMM-lite)، long/short متقارن، تشخیص liquidity sweep
4. **عملیات:** خزانهٔ رمزنگاری‌شدهٔ رازها، بکاپ خودکار روزانه، صف هشدار تلگرام

---

## ۶) هشدار نهایی

این سامانه تصمیم‌یار است، نه تضمین سود. امتیاز بالا احتمال سود نیست.
استقرار واقعی باید با paper trading و اعتبارسنجی خارج از نمونه همراه باشد.
