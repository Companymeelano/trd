<?xml version="1.0" encoding="utf-8"?>
<!--
  مانيفست بستهٔ اندروید — میلانو تریدینگ اینتلیجنس
  این بسته یک WebView تمام‌صفحه امن برای نسخهٔ وب پنل ترید است
  (SSL اجباری، Safe Browsing، دانلود با DownloadManager، لینک خارجی در مرورگر).
  Meelano Studio Design — Milad Yaghoobi
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Meelano.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Meelano.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
